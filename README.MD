# Set up
I use the [uv](https://docs.astral.sh/uv/) package manager and so should u :)

To nstall the necessary package:
```
uv pip install -r requirment.txt
```
and run `python headTracker/headCubed.py` which uses computers default webcam



